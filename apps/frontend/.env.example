# Frontend environment template. Copy to .env.local for development.
# In dev the Vite proxy forwards /api to this target (see vite.config.ts).
VITE_API_TARGET=http://localhost:8080

# Base path the app calls for the API. Leave as /api to use the dev proxy.
VITE_API_BASE_URL=/api
